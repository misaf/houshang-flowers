# API Guide

Use this file as the entry point for Laravel JSON:API work. Read the always-read files, then only the focused files that match the task.

## Always Read For API Work

- `docs/api/contract.md` for JSON:API, Laravel JSON:API, and backend source-of-truth rules.
- `docs/api/layer.md` for API layer organization, JSON:API handling, resources, and type safety.

## Read When Relevant

- `docs/api/configuration.md` for API and storage environment variables.
- `docs/api/errors-and-flow.md` for error handling and frontend data flow.

## Source of Truth

These API docs supplement:

- `docs/project.md`
- `docs/workflow.md`
- `docs/frontend.md`

If guidance appears to overlap, apply the more specific document for the file or feature being changed.
