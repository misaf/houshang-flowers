# Workflow Guide

Use this file as the entry point for agent workflow rules. Read the always-read files, then only the focused files that match the task.

## Always Read For Code Changes

- `docs/workflow/before-editing.md` for pre-editing checks and scope selection.
- `docs/workflow/editing.md` for search, editing, TypeScript, and UI preservation rules.
- `docs/workflow/existing-changes.md` for dirty-worktree behavior.

## Read When Relevant

- `docs/workflow/verification.md` for lint, typecheck, build, and dev-server verification.
- `docs/workflow/scope-control.md` for keeping changes focused.

## Source of Truth

These workflow docs supplement:

- `docs/project.md`
- `docs/frontend.md`
- `docs/api.md`

If guidance appears to overlap, apply the more specific document for the file or feature being changed.
