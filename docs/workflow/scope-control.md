# Workflow Scope Control

Keep every change intentional.

- Solve the requested problem without introducing unrelated improvements.
- Avoid broad refactoring unless explicitly requested.
- Prefer incremental improvements over large rewrites.
- Leave the codebase cleaner than you found it, but only within the scope of the requested work.
