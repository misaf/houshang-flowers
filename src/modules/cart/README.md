# Cart Module

Owns the cart drawer, cart button, and localStorage-backed cart context.

Import from `@/modules/cart` outside this module. Cart items are client-side
state only and are not persisted to the backend.
