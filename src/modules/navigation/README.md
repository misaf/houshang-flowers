# Navigation Module

Owns the header, footer, global search, language switcher, and theme toggle.

Import from `@/modules/navigation` outside this module. Navigation may consume
other feature public APIs such as product categories and product search; do not
import another module's internals.
