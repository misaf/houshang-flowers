# About Module

Owns the about-us page UI and editorial page sections.

Import from `@/modules/about` outside this module. Keep page copy localized in
`messages/en.json` and `messages/fa.json`; do not invent unverified business
claims.
