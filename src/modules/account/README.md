# Account Module

Owns the user panel, favorites state, and local order-history state.

Import from `@/modules/account` outside this module. This is not real
authentication; favorites and orders are client-side localStorage state.
