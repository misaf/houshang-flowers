# Contact Module

Owns the contact page client UI, contact form schema, contact information
presentation, and the condensed FAQ aside.

Import from `@/modules/contact` outside this module. Verified contact values
come from `src/shared/lib/config.ts`; visible labels and descriptions come from
translations.
